kind: Service
apiVersion: v1
metadata:
  name: wacalog-service
spec:
  selector:
    app: wacalog
  ports:
    - port: 8080
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: wacalog-ingress
spec:
  ingressClassName: nginx
  rules:
    - http:
        paths:
          - path: /wacalog
            pathType: Prefix
            backend:
              service:
                name: wacalog-service
                port:
                  number: 8080
---
{{- if .Capabilities.APIVersions.Has "route.openshift.io/v1" }}
apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: wacalog
spec:
  to:
    kind: Service
    name: wacalog-service
  port:
    targetPort: 8080
  tls:
    termination: edge
    insecureEdgeTerminationPolicy: Redirect
---
apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: wacalog-apps-route
spec:
  host: {{ .Values.routerHost | quote }}
  path: /wacalog
  to:
    kind: Service
    name: wacalog-service
  port:
    targetPort: 8080
{{- end }}