# OutbackCDX Index

The replay index of the webarchive infrastructure is responsible keep and index of the WARC files, so that it can be read by the playback engine.

Currently, it is just an *outback cdx*.
